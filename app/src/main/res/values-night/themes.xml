<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.TimeManager" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/white</item>
        <item name="colorPrimaryVariant">@color/gray</item>
        <item name="colorOnPrimary">@color/background_gray</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_200</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">@color/gray_deeper</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.TimeManager.ActionBar" parent="Widget.AppCompat.ActionBar">
        <item name="background">@color/gray</item>
    </style>

    <style name="Theme.TimeManager.FloatingButton" parent="Theme.TimeManager">
        <item name="colorOnSecondary">@color/white</item>
    </style>

    <style name="Theme.FloatingActivity" parent="Theme.AppCompat.Dialog">
        <item name="windowNoTitle">true</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowCloseOnTouchOutside">true</item>
        <item name="android:background">@color/gray</item>
    </style>

    <style name="Theme.Widget.TextView.HeadLine" parent="Widget.AppCompat.TextView">
        <item name="android:textColor">@color/white</item>
    </style>

    <style name="Theme.Widget.TextView.SubText" parent="Widget.AppCompat.TextView">
        <item name="android:textColor">@color/gray_light</item>
    </style>
</resources>